import React from "react";
import { Container, Navbar } from "react-bootstrap";

export const Footer = () => {
  return (
    <Navbar fixed={"bottom"} bg={"light"}>
      <Container>
        <p>Footer</p>
      </Container>
    </Navbar>
  );
};
